# used to store construction project data
# used to store resource utilization
# used to store contract company details

import os


def connect(dbPath: str = os.getenv("POSTGRESQL_DB_URI")):
    pass
